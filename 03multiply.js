// create a function multiply, that will
// receive 2 numbers as arguments and return them multiplied.

const multiply = (number1, number2) => {
    return number1*number2;
}

console.log(multiply(4,8));